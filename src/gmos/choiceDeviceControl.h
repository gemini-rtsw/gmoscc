!
!  DAO deviceControl Record multiple choice field definitions
!
!  Used by deviceControlRecord.ascii and choiceRec.ascii to select 
!  appropriate multiple choice set for each of the following fields:
!

#ifndef INCchoiceDeviceControlh

#define DDR_DEVCONTROL_DIR      0
#define DDR_DEVCONTROL_MODE     1
#define DDR_DEVCONTROL_BUSY     2
#define DDR_DEVCONTROL_SIM      3
#define DDR_DEVCONTROL_DBUG     4
#define DDR_DEVCONTROL_MIP      5
#define DDR_DEVCONTROL_YN       6

#define INCchoiceDeviceControlh
#endif

